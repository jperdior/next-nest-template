generator client {
  provider = "prisma-client-js"
  output   = "../generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================================
// USER CONTEXT
// Domain: User management and authentication
// ============================================================================

enum UserRole {
  ROLE_USER
  ROLE_ADMIN
  ROLE_SUPERADMIN
}

model User {
  id                     String    @id @default(uuid())
  email                  String    @unique
  name                   String
  passwordHash           String?   // Null if using SSO only
  role                   UserRole  @default(ROLE_USER)
  
  // Google SSO
  googleId               String?   @unique
  avatarUrl              String?
  
  // Email verification
  isEmailVerified        Boolean   @default(false)
  emailVerificationToken String?
  emailVerificationExpiry DateTime?
  
  // Password reset
  passwordResetToken     String?
  passwordResetExpiry    DateTime?
  
  // Account state
  isActive               Boolean   @default(true)
  lastLoginAt            DateTime?
  
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt

  @@index([emailVerificationToken])
  @@index([passwordResetToken])
  @@map("users")
}

// ============================================================================
// FUTURE CONTEXTS
// 
// Add new models here, organized by context with clear comment separators.
// 
// Cross-context relationships example:
//
// model Order {
//   id     String @id
//   userId String
//   user   User   @relation(fields: [userId], references: [id])  // FK works!
//   ...
// }
//
// Domain entities only store the userId, not the full User entity.
// Repositories can use `include: { user: true }` to create value objects
// like OrderCustomerVO with denormalized user data.
// ============================================================================
